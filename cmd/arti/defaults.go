package main

// defaultBaseURL is the server the CLI talks to when neither --base-url nor
// ARTI_BASE_URL is set. Point it at your deployment (or export ARTI_BASE_URL)
// after installing.
const defaultBaseURL = "http://localhost:8090"
